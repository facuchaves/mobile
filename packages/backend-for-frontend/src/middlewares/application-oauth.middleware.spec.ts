import { ApplicationOAuthMiddleware } from './application-oauth.middleware'

describe('OauthMiddleware', () => {
  it('should be defined', () => {
    // expect(new OauthMiddleware()).toBeDefined();
  })
})
