import { CandidatesOauthMiddleware } from './candidates-oauth.middleware'

describe('SessionMiddleware', () => {
  it('should be defined', () => {
    // expect(new CandidatesOauthMiddleware()).toBeDefined();
  })
})
